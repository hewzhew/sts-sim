# Legacy Documentation Snapshot

This directory contains the old `docs/` tree as it existed before the active
documentation rewrite on 2026-06-03.

These files are historical context only. They may mention retired or downgraded
workflows including:

- old watch UI and recording UI
- Workbench, DecisionFrame, DecisionBrief, and LLM reviewer paths
- live CommunicationMod tooling as an active path
- old handoff files and stale command flags
- outdated assumptions about route, reward, and replay workflows

Do not use this directory as the default Codex handoff. Current guidance lives
in `docs/` at the repository root.
