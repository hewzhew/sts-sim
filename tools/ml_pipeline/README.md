`ml_pipeline/` is experimental and not part of the current primary tools workflow.

Status:
- prototype / staging area
- not wired into `sts_tool`, `analysis_cache`, or parity tooling

Keep expectations low: code here should be treated as exploratory until it is connected to the structured toolchain.
