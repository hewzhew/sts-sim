`manual/` contains hand-driven debugging tools that are still useful, but are not part of the main automated pipeline.

Current example:
- `combat_trace.py`

These tools are intended for human inspection rather than cache-building, schema generation, or structured audits.
