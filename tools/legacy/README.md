`legacy/` contains helper scripts that are no longer part of the primary tools workflow.

Current examples:
- `convert_logs.py`
- `validate_transitions.py`

These scripts are kept for historical reference and occasional manual recovery work, but they should not be treated as the preferred path for replay conversion or validation.
