This directory contains retired Markdown parsers that are no longer part of the normal coverage workflow.

Current coverage flow:
- first choice: `tools/analysis_cache/*.json`
- fallback: `tools/source_extractor/output/*.json`

These modules remain only for historical reference while the cleanup is in progress.
