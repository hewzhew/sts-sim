# Collector minion-control implementation plan

1. Add failing threat-profile coverage for Collector.
2. Add failing boss-evidence coverage for semantic AOE under an open Collector control deficit.
3. Add a decision-pipeline regression matching the captured A2F21 comparison: both candidates are
   mainline, but Cleave outranks Battle Trance for known Collector.
4. Implement the shared threat record and bounded boss-specific evidence.
5. Run focused strategy tests, rebuild the fast runner, and rerun seed `20260713002` with the same
   contract.
6. Keep the change only if the rerun preserves the Act 2 clear and improves or matches the prior
   trajectory; otherwise revert the behavior commits.
