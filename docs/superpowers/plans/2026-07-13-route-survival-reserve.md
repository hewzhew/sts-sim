# Route survival reserve implementation plan

1. Add failing route tests proving full-health projected damage retains a cost
   and a three-HP p90 suffix is not labeled `Ok`.
2. Add the baseline HP-conservation need and pass max HP into the safety gate.
3. Run the route suite and rerun seed `20260713002` with typed route packets.
4. Compare A2F17 selection, subsequent attrition, relic development, and the
   next real obstruction before changing another subsystem.
