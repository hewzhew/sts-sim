pub fn on_equip() {
    // Note: Ceramic Fish is Out-of-Combat. During `RunState` deck updates (e.g. `obtain_card`),
    // it checks for this relic and routes +9 through `change_gold_with_source`.
}
