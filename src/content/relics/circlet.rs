pub fn on_equip() {
    // Note: Circlet is stateless and has no direct combat effect. `state.entities.player.relics`
    // simply tracks multiple instances or a counter for visual representation if acquired repeatedly.
}
