// Minion Power is primarily a status / marker buff indicating this entity is a minion.
// Behavior dictates they should flee when the leader dies. Engine cleanup will handle this based on this Power.

pub struct MinionPower;

impl MinionPower {
    // Basic structural stub, not strictly required to have methods unless we add specific hooks.
}
