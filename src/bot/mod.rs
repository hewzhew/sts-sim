//! Deprecated compatibility namespace.
//!
//! New code should use `ai`, `sim`, or `eval` directly. This module only keeps
//! old public paths alive while the architecture is moved off the legacy bot
//! stack.

pub mod combat;
