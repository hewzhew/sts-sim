[package]
name = "sts_combat_planner"
version = "0.1.0"
edition = "2021"
publish = false
autobins = false
autotests = false

[dependencies]
sts_core = { package = "sts_simulator", path = "../.." }
serde = { version = "1.0", features = ["derive"] }
