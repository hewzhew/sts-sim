[package]
name = "sts_oracle_learning_env"
version = "0.1.0"
edition = "2021"
publish = false
autobins = false
autotests = false

[lib]
path = "src/lib.rs"

[dependencies]
sts_oracle_run_control = { path = "../sts_oracle_run_control", default-features = false }
rmp-serde = "1.3"
serde = { version = "1.0", features = ["derive"] }
