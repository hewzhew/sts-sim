[build-system]
requires = ["maturin>=1.12,<2"]
build-backend = "maturin"

[project]
name = "sts-learning-bridge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy>=2.0"]

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "sts_learning_bridge._native"
python-source = "python"
